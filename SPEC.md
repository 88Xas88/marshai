# SPEC.md — Детальное ТЗ Marshai, Этап 1

## 3 страницы которые нужно построить

---

## СТРАНИЦА 1 — Главная (`/`)

### Форма поиска

**Поля:**
- **Откуда** — autocomplete при вводе 2+ символов, список из 50 городов РФ
- **Куда** — autocomplete аналогично. Кнопка "Составить план" `disabled` пока поле пустое
- **Когда · дней** — на десктопе текстовое поле, на мобайле нативный `<input type="date">` + число дней

**Параметры (3 dropdown под полями):**
- Кол-во человек: 1 / 2 / 3 / 4 / 5+
- Бюджет: Эконом / Средний / Свободно
- Интересы: Музеи и история / Рестораны и еда / Прогулки / Всё понемногу

**Поведение:**
- Кнопка `disabled` + серая пока поле "Куда" пустое
- При клике на кнопку с пустым "Куда" — красная обводка поля + red dot рядом с лейблом
- 5 подсказок под формой (кликабельны, заполняют форму): Питер на майские / Сочи летом / Казань на выходные / Калининград / Владивосток
- Touch-targets: **минимум 48px height** на всех интерактивных элементах (стандарт iOS/Android)
- На мобайле форма в 1 колонку, кнопка width: 100%

**При нажатии "Составить план":**
```
router.push(`/generating?from=${from}&to=${to}&dates=${dates}&days=${days}&pax=${pax}&budget=${budget}&interests=${interests}`)
```

### Социальное доказательство
3 карточки реальных планов:
- Маршрут (напр. "Москва → Санкт-Петербург")
- Мета: дни, месяц, кол-во человек
- Теги: транспорт / жильё / кол-во мест
- Итоговая цена зелёным
- Аватар-инициалы с цветом (АИ = teal #E1F5EE, МС = blue #E6F1FB, СИ = amber #FAEEDA)
- Карточки кликабельны → `/plan/example-spb`

### Trust-strip
4 метрики в ряд: `30 сек` / `Реальные цены` / `Карта по дням` / `Бесплатно`

### Секция "Как работает"
4 шага в одной рамке с border: 01 Описываешь → 02 ИИ ищет → 03 Получаешь план → 04 Едешь

---

## СТРАНИЦА 2 — Генерация (`/generating`)

Query params: `from`, `to`, `dates`, `days`, `pax`, `budget`, `interests`

### При монтировании компонента
Немедленно запускается `EventSource('/api/generate/stream?...')` который:
1. Запускает `Promise.allSettled([fetchFlights, fetchHotels, generateItinerary])` на сервере
2. По мере ответа каждого API — отправляет SSE-событие
3. Frontend обновляет `planStore` и статусы шагов
4. По завершении — редирект на `/plan/[id]`

### Левая колонка — шаги

**Countdown таймер:** `~28 сек` → `~27 сек` → ... → `Готово!`

**6 шагов с статусами (pending → active → done):**
1. Запрос принят — сразу done
2. Билеты найдены — done когда Flights API ответил
3. Подбираем жильё — active пока Hotels API грузится
4. Строим маршрут по дням — pending → active → done
5. Формируем карту — pending → active → done
6. Финальный план — done последним

**Анимации шагов:**
- pending: серый кружок с точкой
- active: зелёный кружок с пульсирующей точкой
- done: тёмный (#2C2C2A) кружок с белым чекмарком, fade-in 200ms

**После 15 секунд** — появляется кнопка "Показать что уже нашли" → переход на результат с частичными данными

### Цепочка вопросов (RETENTION +8%)

Три вопроса появляются поочерёдно пока идёт генерация:

**Вопрос 1 (сразу):** "Пока ищем — уточним план. Что важнее в поездке?"
- Музеи и история / Рестораны и еда / Прогулки и природа / Шоппинг / Всё понемногу

**Вопрос 2 (через 3 сек после ответа):** "Какой темп предпочитаешь?"
- Насыщенный — максимум мест / Размеренный — без спешки / Смешанный
- Предыдущий блок: opacity → 0.5

**Вопрос 3 (через 3 сек):** "Бюджет на день (без жилья)?"
- до 2 000 ₽ / до 4 000 ₽ / Свободно
- После ответа: "Всё учтём — план будет именно под тебя ✓"

Ответы передаются в Claude API как дополнительный контекст.

### Правая колонка — предпросмотр

Блоки появляются по мере ответа API (анимация: opacity 0→1, translateY 6px→0, 300ms):
- **Блок транспорта** — 3 строки с бейджами (Лучший/Быстрее/Дешевле) и ценами
- **Блок отелей** — первый найденный + скелетон-анимация остальных
- **Блок маршрута** — День 1 + скелетоны

---

## СТРАНИЦА 3 — Результат (`/plan/[id]`)

### Навбар (sticky при скролле)
- Логотип Marshai
- Якорные табы: Транспорт / Жильё / Маршрут / Карта (smooth scroll к секциям)
- Кнопки: Сохранить (outline) / Поделиться (тёмная)

### Trip-bar под навбаром
- h2: "Откуда → Куда"
- Мета: даты · дни · человек · цена от X ₽ (зелёным)
- Кнопка "Перегенерировать"

### БЛОК 1 — "Оптимальный план" (ПЕРВЫЙ на странице, ОБЯЗАТЕЛЬНО)
```
Тёмная шапка (#2C2C2A):
  "Оптимальный план — рекомендуем"
  "ИИ выбрал лучшее соотношение цены, удобства и рейтинга"

Белое тело:
  3 ячейки рядом:
  [Транспорт туда]    [Жильё · 5 ночей]    [Транспорт обратно]
  Название            Название               Название
  Обоснование (1 стр) Обоснование (1 стр)   Обоснование (1 стр)
  Цена                Цена                   Цена

  Итого: X ₽    [Забронировать всё →]
```
Обоснование — короткая фраза почему выбрали именно это: "центр→центр, без пересадок", "8.7★ · центр · отмена бесплатно"

### БЛОК 2 — Транспорт туда (id="transport")
- Табы: Все / Поезд / Самолёт / Автобус
- 3 лучших варианта:
  - Бейдж (Лучший=green / Быстрее=blue / Дешевле=amber)
  - Время отправления → прибытия
  - Название рейса и маршрут
  - Длительность
  - Цена от X ₽
  - Кнопка "Купить →" (тёмная)
- Ссылка "Все варианты →"

### БЛОК 3 — Жильё (id="hotels")
- 2 варианта с полными данными:
  - Название + звёзды + рейтинг + кол-во отзывов
  - Теги: Центр / Завтрак / Wi-Fi / Бесп. отмена
  - Цена/ночь и итого за все ночи
  - Кнопка "Бронировать →" (тёмная)
  - Кнопка "Заменить отель" (мелкая, под кнопкой) — подставляет следующий из кеша
- Ссылка "Все варианты →"

### БЛОК 4 — Маршрут по дням (id="itinerary")
- День 1 раскрыт по умолчанию, дни 2-5 свёрнуты
- Каждый день: бейдж "День N" + заголовок + дата + chevron (rotate 0→90deg при раскрытии)
- Внутри дня — список POI:
  - Время + цветная точка (синий=музей, оранжевый=ресторан, зелёный=прогулка) + соединяющая линия
  - Название + кнопка "заменить" (мелкая серая)
  - Мета: длительность · цена · адрес
  - Тег типа
- Кнопка "заменить" открывает модал с 3 альтернативами из кеша (без повторной генерации)

### Sticky CTA-бар (ОБЯЗАТЕЛЬНО, без rage-click)

**Поведение:**
- Появляется через 300px скролла от верха (slide-up 200ms)
- Скрывается когда юзер доскроллил до блока `#save-plan`
- Основной контент: `padding-bottom: 64px` чтобы бар не перекрывал контент

**Desktop layout:**
```
[Транспорт туда: Сапсан · 10 мая]  [2 890 ₽]  [Купить →]
|
[Жильё · 5 ночей: Апарт.]         [16 000 ₽] [Бронировать →]
|
[Итого транспорт + жильё]          [21 780 ₽] [Всё сразу → (зелёная)]
```

**Mobile layout:** только одна строка "Итого от X ₽ → Забронировать"

### Success overlay (RETENTION +7%)

При клике на ЛЮБУЮ кнопку "Купить →" или "Бронировать →":
1. Ссылка открывается в `target="_blank"`
2. Появляется overlay в правом нижнем углу (НЕ блокирующий, НЕ fixed):
```
┌─────────────────────────────────┐
│ Открыли в новой вкладке ✓       │
│ Сохранить остальной план?       │
│ [email поле] [Сохранить] [✕]   │
└─────────────────────────────────┘
```
3. Автоскрытие через 8 секунд если нет взаимодействия

### Правая колонка (sticky top: 56px)

**Карта Яндекс:**
- 300×180px, Яндекс Maps JS API
- Метки по типу POI: синий=музей, оранжевый=ресторан, зелёный=прогулка, красный=отель
- Табы дней (1 / 2 / 3 / 4 / 5 / Все) — переключение перемещает карту к точкам дня
- Не скрывать логотип Яндекса

**Бюджет:**
- 5 строк: транспорт туда / обратно / жильё X ночей / питание ~X ₽ / музеи ~X ₽
- Итого: крупно зелёным
- Сноска: "питание и музеи — приблизительно"

**Совет ИИ:**
- Фиолетовая иконка, текст о срочности ("цены на Сапсан выросли на 12% за 3 дня")

**Сохранить план (id="save-plan"):**
- Поле email + кнопка "Сохранить"
- Подпись: "Пришлём ссылку + уведомим если цены изменятся"
- При submit: INSERT в plans + email через Resend

### Empty states (ОБЯЗАТЕЛЬНО)

| Ситуация | Что показать |
|---|---|
| Нет билетов на дату | "Прямых рейсов не нашлось — попробуем ±1 день?" + кнопки дат |
| API timeout (>10 сек) | Скелетон + "Загружаем..." + кнопка "Обновить" |
| Нет отелей в бюджете | "Ничего нет в этом бюджете — расширить фильтр?" |
| Claude API ошибка | "Не удалось построить маршрут — попробуем ещё раз?" + кнопка |

---

## API интеграции

### Travelpayouts — Flights API
```
GET https://api.travelpayouts.com/v1/prices/cheap
Params: origin, destination, depart_date, return_date, currency=rub, token
Лимит: 200 req/час на IP
Новая версия API (ноябрь 2025) — использовать именно её
```

### Travelpayouts — Hotels (Hotellook)
```
GET https://engine.hotellook.com/api/v2/cache.json
Params: location, checkIn, checkOut, adults, currency=rub, token, limit=10
```

### Яндекс Расписания
```
GET https://api.rasp.yandex.net/v3.0/search/
Params: from, to, date, transport_types, apikey
Обязательно: надпись "Данные предоставлены Яндекс.Расписаниями" на сайте
```

### Claude API (маршрут)
```typescript
const response = await anthropic.messages.create({
  model: "claude-sonnet-4-20250514",
  max_tokens: 4000,
  messages: [{
    role: "user",
    content: buildItineraryPrompt(params, engageAnswers)
  }]
})
// Парсим строгий JSON из response.content[0].text
```
Ключ ТОЛЬКО на сервере, никогда в браузере.

### Партнёрские ссылки
```typescript
function buildAffiliateUrl(url: string): string {
  const marker = process.env.TRAVELPAYOUTS_MARKER
  return `${url}${url.includes('?') ? '&' : '?'}marker=${marker}`
}
```

---

## Дизайн-система

### Цвета (СТРОГО)
| Назначение | Цвет |
|---|---|
| Primary actions (кнопки, CTA) | `#2C2C2A` тёмно-серый |
| Цены, статус "успех", done | `#1D9E75` зелёный |
| Фон карточек | `var(--color-background-primary)` |
| Фон страниц | `var(--color-background-tertiary)` |
| Границы | `0.5px solid var(--color-border-tertiary)` |

### Радиусы
- Элементы: `8px`
- Карточки: `12px`
- Пилюли/бейджи: `20px`

### Типографика
- font-weight: **400** обычный, **500** жирный — никогда 600/700
- Минимальный размер текста: **11px**
- Заголовки: letter-spacing -0.3px

### Анимации
| Элемент | Анимация |
|---|---|
| Autocomplete dropdown | появление 120ms ease-out |
| Шаг pending→active | dot пульсирует |
| Шаг active→done | чекмарк fade-in 200ms |
| Блоки предпросмотра | opacity 0→1, translateY 6→0, 300ms |
| Chevron дней | rotate 0→90deg, 200ms |
| Sticky CTA появление | slide-up 200ms |
| Success overlay | fade-in снизу-справа 200ms |
| Скелетон | shimmer 1.3s infinite |

### Запрещено
- Никаких градиентов
- Никаких drop-shadow
- Никаких blur / glow
- Только flat дизайн

---

## Мобайл (КРИТИЧНО — 62% трафика)

- Touch-targets: **min-height 48px** везде
- Поле "Когда": `<input type="date">` нативный пикер
- Форма: grid 1 колонка
- Кнопка "Составить план": `width: 100%`
- Sticky CTA: только 1 строка "Итого от X ₽ → Забронировать"
- Breakpoints: mobile `<768px` / tablet `768–1024px` / desktop `>1024px`

---

## Чеклист — без этого не сдаём

- [ ] Autocomplete городов при 2+ символах
- [ ] Кнопка disabled до заполнения "Куда"
- [ ] Параллельные API запросы (Promise.allSettled)
- [ ] SSE прогресс шагов
- [ ] Countdown + цепочка 3 вопросов на генерации
- [ ] Блок "Оптимальный план" ПЕРВЫЙ на результате
- [ ] Обоснование выбора под каждым элементом оптимального плана
- [ ] Sticky CTA с padding-bottom 64px (без rage-click)
- [ ] Success overlay при клике "Купить"
- [ ] Touch-targets 48px везде
- [ ] Empty states при ошибках API
- [ ] Партнёрские маркеры в ссылках
- [ ] Яндекс карта переключается по дням
- [ ] Замена POI и отеля из кеша
- [ ] Email-сохранение пишет в БД + отправляет письмо
- [ ] Демо-план /plan/example-spb для карточек главной

